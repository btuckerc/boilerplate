#!/bin/bash
# Managed by chezmoi - https://github.com/btuckerc/boilerplate
# Bash login shell configuration

# Source .bashrc for login shells (ensures interactive config is loaded)
[ -r "$HOME/.bashrc" ] && . "$HOME/.bashrc"
