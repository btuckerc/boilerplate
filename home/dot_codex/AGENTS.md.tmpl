# Global Agent Instructions

Shared Codex state is managed with chezmoi.

- Edit shared Codex files in `~/src/boilerplate/home/dot_codex/` or `{{ .decentangl.defaults.codex_source }}`, then apply with `chezmoi`.
- Keep `{{ .decentangl.defaults.codex_home }}/auth.json`, history, caches, sqlite files, and project trust local.
- Use `mise` for `omp`, `bw`, `restic`, and `chezmoi`. Use the native package manager for OS packages.
- On macOS with iCloud Desktop & Documents, keep active repos, build trees, virtualenvs, and Codex task workspaces under `~/src`, not `~/Documents`.
- Prefer Tailscale SSH targets for remote access: `omarchy`, `macbook`, `admin@macmini`.
- OMP is the primary cross-platform terminal harness. Codex Desktop remains usable as a local app, but the Codex CLI, vanilla Pi, RTK, and OpenCode are not part of the active shared baseline.
- For machine settings, native app preferences, or platform-specific operations, use the shared `platform-ops` skill.
- Keep shared guidance short and durable. Let chezmoi render platform or machine specifics into the live files.

Current target:
{{ template "codex/current-target.md.tmpl" . }}
