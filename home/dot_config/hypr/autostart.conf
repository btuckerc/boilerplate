# Extra autostart processes
exec-once = uwsm-app -- /home/tux/.local/bin/hyprland-workspace-compactd
# Omarchy uses SDDM autologin, so start locked and let hyprlock handle unlock.
exec-once = hyprlock || hyprctl dispatch exit
