# Change the default Omarchy look'n'feel

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    gaps_in = 2
    gaps_out = 4

    # Change to niri-like side-scrolling layout
    # layout = scrolling
}

# Remove gaps and border when a workspace has a single tiled window.
workspace = w[t1] s[false], gapsout:0, gapsin:0, bordersize:0
workspace = w[tg1] s[false], gapsout:0, gapsin:0, bordersize:0
workspace = f[1] s[false], gapsout:0, gapsin:0, bordersize:0

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 6

    shadow {
        enabled = false
    }

    blur {
        enabled = false
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = no
}

# https://wiki.hypr.land/Configuring/Variables/#layout
layout {
    # Avoid overly wide single-window layouts on wide screens
    # single_window_aspect_ratio = 1 1
}

misc {
    # Keep background Codex/terminal workspaces from stealing focus.
    focus_on_activate = false
}

windowrule = opacity 1.0 1.0, match:tag default-opacity
