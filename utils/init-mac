#!/bin/bash
# Copyright (c) 2024 Tucker Craig
# See LICENSE file for full license details.

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_DIR="$(cd "$SCRIPT_DIR/.." && pwd)"

cat <<EOF >&2
Deprecated: utils/init-mac no longer matches the chezmoi-managed repo layout.
Use "$REPO_DIR/setup" or "chezmoi init --apply https://github.com/btuckerc/boilerplate.git" instead.
EOF
exit 1
